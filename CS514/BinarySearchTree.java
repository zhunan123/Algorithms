package CS514;

public class BinarySearchTree {

    /**
     * 输入:
     * 一棵如下所示的二叉树: 如果9在这棵树中就true
     *        8
     *      /   \
     *     4     10
     *    /    /  \
     *   1    9   12
     * d = 9
     * 输出:
     *true
     * */

/*    public String find(String element) {
        if (this.isLeaf()) {
            if (this.data.equals(element)) {
                return element;
            } else {
                return null;
            }
// data to be found is less than this node
        } else if (this.data.compareTo(element) > 0) {
            Return left.find(element);
        } else {
            Return right.find(element);
        }
    }*/
}
